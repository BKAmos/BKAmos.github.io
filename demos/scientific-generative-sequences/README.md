# Generative sequences (PWM + latent)

Classical motif PWM sampling plus a 2D latent “decoder” that modulates PWM–background mixing; log-odds scores and interpolation along a latent path.

```bash
python3 data/generate.py
python3 src/run.py
```
