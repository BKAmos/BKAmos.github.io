# Residue contact map (synthetic)

Pairwise Cα distances for a short chain; heatmap and binary contacts at 8 Å (excluding sequential neighbors).

```bash
python3 data/generate.py
python3 src/run.py
```
