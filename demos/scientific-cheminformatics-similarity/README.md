# Compound similarity (synthetic, no RDKit)

64-bit binary fingerprints from scaffold families; Tanimoto to a query compound and 2D PCA embedding.

```bash
python3 data/generate.py
python3 src/run.py
```
